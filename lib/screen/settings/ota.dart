import 'package:flutter/material.dart';

class SystemUpdatePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Aggiornamento sistema'),
      ),
      body: const Center(
        child: Text('Contenuto della pagina di aggiornamento del sistema'),
      ),
    );
  }
}
